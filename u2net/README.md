# U2Net Model Assets

This folder stores the U2Net ONNX model used by the virtual try-on pipeline.

## Contents

- `u2net.onnx`: segmentation/model asset used by the try-on workflow.

Keep this file together with the model asset so the VITON-HD pipeline can find the required file during preprocessing or inference.
